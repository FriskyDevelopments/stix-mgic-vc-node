# Room admin commands

Dashboard panel for live room control:

- mute / unmute participant
- kick
- pin message
- end call

All via the existing adapter, exposed through `/v1/rooms/:id/admin/*`.

## Status

Spec only. Dashboard panel and adapter methods not coded.